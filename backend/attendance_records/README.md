# Attendance Records

This folder contains attendance logs generated automatically by the system.

- Files are named by date: `YYYY-MM-DD.json`
- Each file contains a `records` object mapping student names to timestamps
- Excel exports (`*.xlsx`) can be generated via the `/export` API endpoint

Do not manually edit these files.
